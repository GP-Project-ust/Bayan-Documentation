# LaTeX intermediate files — don't commit these
*.aux
*.toc
*.lof
*.lot
*.out
*.log
*.xdv
*.fls
*.fdb_latexmk
*.synctex.gz
*.bbl
*.bcf
*.run.xml
*.blg

# Editor / OS files
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
.idea/

# Python (for scripts)
__pycache__/
*.pyc

# NOTE: main.pdf IS tracked intentionally — the CI workflow rebuilds and
# pushes it back to the repo on every change to sources. Do not re-add it
# here (a tracked file stays tracked even if ignored).
